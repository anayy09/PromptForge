import { ChatView } from "@/components/chat/ChatView";

export const metadata = {
  title: "Chat — PromptForge",
  description: "An everyday multimodal assistant powered by the model registry.",
};

export default function ChatPage() {
  return <ChatView />;
}
